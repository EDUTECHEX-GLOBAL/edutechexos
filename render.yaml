services:
  - type: web
    name: edutechexos-backend
    env: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: "10002"
    healthCheckPath: /health
    # optional: autoDeploy: true
